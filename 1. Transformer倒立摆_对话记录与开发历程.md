# Transformer 控制倒立摆 · 对话记录与开发历程

> 本文档记录本项目从需求提出到最终交付的完整对话过程、关键决策、问题排查与技术洞察。

---

## 0. 对话总览

本次对话围绕**倒立摆控制**与**Transformer**两条主线展开，经历了八个阶段，从"通用 Transformer 实现指南"逐步收敛到"用 Transformer 控制倒立摆"这一完整项目，最终沉淀为网页、训练脚本、技术文档与说明文档四类交付物。

```
阶段一：Transformer 实现指南（MATLAB + Python）
   ↓
阶段二：LQR 控制倒立摆网页
   ↓
阶段三：Transformer 控制倒立摆（澄清需求后重构）
   ↓
阶段四：PDF/LaTeX 技术文档
   ↓
阶段五：文档扩充（更详细 + 配图 + 训练指南）
   ↓
阶段六：编译错误排查与修复（多轮）
   ↓
阶段七：理论问答（可解释性、注意力调参）
   ↓
阶段八：网页分析模块（敏感度 + 诊断）+ 项目总结
```

---

## 1. 阶段一：Transformer 实现指南（MATLAB + Python）

**用户需求**：MATLAB、Python 分别实现 Transformer 网络，给出实际应用案例，生成 md 说明文档。

**实现与产出**：
- 生成 `Transformer实现指南_MATLAB与Python.md`
- 关键点：核实了 **MATLAB 官方并无内置 `transformerLayer`**，需用 `selfAttentionLayer`（R2023a+）+ `positionEmbeddingLayer`（R2023b+）组合搭建；Python 侧用 PyTorch 从零手写 + `nn.Transformer` + Keras。

**追加需求**：从零搭建 + 旧版本实现。
- 补充了 MATLAB **R2019b+** 用 `dlarray` + `dlfeval` + `dlgradient` + `adamupdate` 自定义训练循环的完整从零实现（手写多头注意力、LayerNorm、FFN、位置编码），不依赖任何内置 Transformer 层。

---

## 2. 阶段二：LQR 控制倒立摆网页

**用户需求**：实现控制倒立摆，网页展示原理、伪代码、流程图、实时计算步骤。

**实现与产出**：
- 生成 `inverted_pendulum.html`（LQR 控制倒立摆，深色工业仪表台风格）
- 核心：Cart-Pole 物理建模 + LQR（离散 Riccati 迭代）+ RK4 积分 + Canvas 动画
- 验证：LQR 增益 K 解出，θ 从 0.15 rad 收敛到 0

---

## 3. 阶段三：Transformer 控制倒立摆（关键转折）

**用户澄清**：不是用 LQR，而是**用 Transformer 网络本身作为倒立摆控制器**。

**关键决策**：
1. **环境探测**：确认本地有 Python 3.12 + NumPy，无 torch。
2. **方案确定**：用**纯 NumPy 手写 Transformer**（前向 + 反向传播），通过**模仿学习**训练（LQR 为专家教师），导出权重内嵌网页。
3. **保证可行性**：LQR 专家保证稳定，Transformer 学其策略；数据加扰动以覆盖偏离平衡的状态。

**实现与产出**：
- 生成 `train_transformer_controller.py`（约 16KB，单文件自包含）
- 手写反向传播：softmax / 注意力 / LayerNorm / FFN / 嵌入各层解析梯度
- **梯度检查**：最大相对误差 < 1e-9（除 `bk` 外全部精确匹配，`bk` 是梯度接近 0 的数值梯度假阳性）
- 训练结果：5.8 万样本 / 60 epoch / loss 0.92 → 10⁻³
- **闭环验证**：θ₀=0.1/0.2/0.3 rad 均稳定（峰值 5.7°/11.4°/17.1°）
- 生成 `transformer_pendulum.html`（内嵌权重 + 注意力热力图）

---

## 4. 阶段四：PDF/LaTeX 技术文档

**用户需求**：写 PDF 或 LaTeX 文档，详细介绍 Transformer 控制倒立摆，含理论原理、公式、代码说明。

**实现与产出**：
- 生成 `transformer_cartpole.tex`（LaTeX 源文件）
- 生成 `transformer_cartpole.pdf`（reportlab + matplotlib mathtext，环境无 LaTeX 编译器）
- 内容：倒立摆建模、LQR、Transformer 理论、网络架构、编码实现、实验、结论

---

## 5. 阶段五：文档扩充

**用户需求**：更详细地介绍建模过程、LQR 原理、Transformer 原理及各部分编码。

**扩充内容**：
- **建模**：完整拉格朗日推导（动能展开 → 两个运动方程 → 显式消元）、泰勒展开线性化（含"ẍ 线性化须代入线性化后的 θ̈"这一关键耦合）、可控性分析（det C = g²/M⁴l⁴）
- **LQR**：庞特里亚金极小值原理完整推导（哈密顿 → 协态方程 → 最优性条件 → CARE）
- **Transformer**：√d_k 方差分析、多头动机、位置编码设计
- **编码**：完整反向传播（attention_backward、layernorm_backward）+ 梯度检查

**后续追加**：用 TikZ 画三张图（倒立摆示意图、控制闭环图、Transformer 原理图）+ 手动训练运行指南（六阶段顺序）。

---

## 6. 阶段六：编译错误排查（多轮，重要）

用户多次发来编译错误截图，逐一定位修复：

| 错误 | 根源 | 修复 |
|------|------|------|
| `xcolor: Undefined color '80'` | TikZ 里误写 `draw=red!70!80`（`80` 被当颜色名） | 改 `red!70!black` |
| `Missing $ inserted.` | **反引号 `` ` `` 标注函数名**（反引号是 LaTeX 重音符号，导致 `_` 暴露在文本模式） | 全部改 `\texttt{}`（下划线转义 `\_`） |
| hyperref 警告（PDF 书签） | 章节标题含 `$...$` 数学模式 | 用 `\texorpdfstring{数学}{纯文本}` 包裹 |
| 代码块中文注释 | lstlisting 等宽字体不支持中文 | 31 处中文注释改英文 |

**排查过程中的教训**：shell 内联 Python 命令的转义层级易出错（反斜杠丢失导致 `\texttt` 变 tab+extt），改用脚本文件方式修复。

---

## 7. 阶段七：理论问答（两个有深度的问题）

### 7.1 为什么 Transformer 控制有可解释性？

**核心回答**：
- Transformer 的可解释性来自**注意力权重的显式性**——推理时强制产出一组"谁关注谁"的概率分布，可直接可视化并与物理直觉对照。
- 对比：LQR 可解释性来自**线性结构的分离性**（每个增益标量物理意义明确）；MLP 是**黑箱**（稠密矩阵耦合）。
- **诚实边界**：注意力权重 ≠ 因果贡献（Value 还要经后续线性变换），注意力是"有限但实用的诊断窗口"，非严格因果可解释性。

### 7.2 可以根据注意力矩阵调参吗？

**核心回答**：可以，但注意力是**诊断信号**而非**优化目标**。

给出了四类信号 → 超参数的映射：
- 关注范围 → 序列长度 T
- 头冗余度 → 头数 h
- 注意力尖锐度 → 每头维度 d_k
- 注意力随训练收敛 → 学习率

并强调最终以闭环稳定性/loss 为准。

---

## 8. 阶段八：网页分析模块 + 项目总结

### 8.1 输入敏感度分析（扰动法）

**用户需求**：在网页加分析模块，展示"注意力 ≠ 因果"。

**实现**：对每个输入位置（8 步 × 4 分量）加扰动，测输出变化率 ∂u/∂z，渲染为 8×4 热力图（红=正敏感、蓝=负敏感）。

**关键实证结果**：

| 控制器 | 当前步敏感度 | 历史步敏感度 |
|--------|-------------|-------------|
| LQR | [2.95, 4.73, 46.34, 9.16] | **全 0** |
| Transformer | [1.98, 3.57, 31.19, 6.87] | **非零** |

三条结论：Transformer 学到了 LQR 策略（结构一致）；Transformer 区别于 LQR 在于利用历史；注意力与敏感度对照实证"注意力 ≠ 因果"。

### 8.2 注意力诊断面板

**用户需求**：自动统计集中度、头相似度、关注范围，输出调参建议。

**实现**：四个指标（归一化熵、关注跨度、最近 3 步占比、头相似度）+ 规则化建议生成。

**开发中的关键修正**：初版"95% 覆盖 + 熵>0.8"导致**平衡态误报欠拟合**（实测发现该模型注意力天然偏分散，熵≈0.82）。改用"最近 3 步占比"（区分度更高）+ 熵阈值放宽到 0.95。边界测试全部正确。

### 8.3 项目总结

生成 `Transformer倒立摆控制_项目说明.md`，梳理 8 个核心文件、依赖关系、核心成果、复现步骤与常见问题。

---

## 9. 关键技术洞察汇总

1. **MATLAB 无内置 transformerLayer**——须组合 `selfAttentionLayer` 等基础层（R2023a+），或 R2019b+ 用 `dlarray` 自定义训练循环从零搭建。

2. **质点杆模型使非线性动力学与线性化严格自洽**——线性化时"ẍ 须代入线性化后的 θ̈"（因 θ̈ 本身含 u），这一耦合常被忽略。

3. **模仿学习的可行性保障**——LQR 专家保证稳定 + 数据加扰动覆盖偏离平衡态 + 梯度检查验证手写反向传播，三者缺一不可。

4. **注意力 ≠ 因果**——注意力权重是相关性/关注倾向，严格归因需梯度方法或扰动法；网页通过两图并排给出直观实证。

5. **注意力调参是诊断而非优化**——注意力模式能提示方向（T、h、d_k），但最终裁判是闭环稳定性；且注意力分布受状态本身影响（平衡态天然分散），需谨慎解读。

6. **LaTeX 反引号陷阱**——`` ` `` 在 LaTeX 是重音符号而非代码标注，标注函数名必须用 `\texttt{}`（下划线转义 `\_`）。

---

## 10. 最终交付物总览

| 类别 | 文件 | 说明 |
|------|------|------|
| 交互网页 | `transformer_pendulum.html` | 内嵌权重，含动画/注意力/敏感度/诊断 |
| 训练脚本 | `train_transformer_controller.py` | 纯 NumPy，六阶段自包含 |
| 模型权重 | `transformer_weights.json`、`weights_compact.json` | 完整版 + 紧凑版 |
| 技术文档 | `transformer_cartpole.tex`、`transformer_cartpole.pdf` | 理论+公式+代码+配图 |
| 生成脚本 | `generate_pdf.py`、`transformer_pendulum_template.html` | PDF 生成 + 网页模板 |
| 说明文档 | `Transformer倒立摆控制_项目说明.md` | 项目总览与复现 |
| 本记录 | 本文档 | 对话历程与决策 |
| 前序产物 | `inverted_pendulum.html`、`Transformer实现指南_MATLAB与Python.md` | LQR 版网页 + 早期指南 |

---

*本文档为 AI 助手与用户的完整协作记录，覆盖需求澄清、方案设计、实现、验证、问题排查与理论讨论全过程。*
